#include <stdio.h>
int main()
{
    int arr[5] = {10, 20, 30, 40, 50};
    int *ptr;
   ptr = arr;  
printf("Using pointer arithmetic:\n");
for(int i = 0; i < 5; i++)
    {
printf("Value = %d, Address = %p\n", *(ptr + i), (ptr + i));
    }
 return 0;
}
